import { Box } from "@material-ui/core"
import { MovieInfo } from "../../../domain/movie_repository/model/movie_info_model"

const CastList = (movieInfo: MovieInfo) => {
  return <Box></Box>
}

export default CastList
